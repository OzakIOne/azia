{
  "name": "azia",
  "version": "1.0.0",
  "description": "Small script that open a youtube video every X minutes",
  "main": "index.ts",
  "type": "module",
  "dependencies": {
    "@inquirer/prompts": "^8.1.0",
    "open": "^11.0.0"
  },
  "devDependencies": {
    "@types/inquirer": "^9.0.9",
    "@types/node": "^25.0.3",
    "bun-types": "^1.3.5"
  },
  "scripts": {
    "start": "bun run index.ts",
    "build": "bun build ./index.ts --outdir ./dist --target bun",
    "compile": "bun build ./index.ts --compile --outfile azia",
    "compile:windows": "bun build --compile --target=bun-windows-x64 ./index.ts --outfile bin/windows/azia.exe",
    "compile:linux": "bun build --compile --target=bun-linux-x64 ./index.ts --outfile bin/linux/azia",
    "compile:macos": "bun build --compile --target=bun-macos-x64 ./index.ts --outfile bin/macos/azia",
    "clean": "rm -rf dist/ bin/ azia"
  },
  "author": "ozaki",
  "license": "MIT"
}
